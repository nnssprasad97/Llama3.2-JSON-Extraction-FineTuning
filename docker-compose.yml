services:
  app:
    build: .
    ports:
      - "7861:7860" # Map to 7861 to avoid common 7860 conflicts
    environment:
      - GRADIO_SERVER_NAME=0.0.0.0
      - GRADIO_SERVER_PORT=7860
    volumes:
      - .:/app
